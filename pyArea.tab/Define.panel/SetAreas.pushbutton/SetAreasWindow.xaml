<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Area Usage Types" 
        Width="550" Height="400"
        MinHeight="300" MaxHeight="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Background="#F5F5F5">
    <Grid Margin="20" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
    <StackPanel Grid.Row="0">
        <TextBlock Text="Usage Type:" 
                   FontSize="14" 
                   FontWeight="Bold"
                   Margin="0,0,0,5"/>
        
        <Grid Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="combo_usage_type" 
                      Grid.Column="0"
                      Height="30"
                      FontSize="13"
                      IsEditable="True"
                      IsTextSearchEnabled="False"
                      Margin="0,0,10,0"
                      FlowDirection="RightToLeft"
                      Background="White">
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ComboBox.ItemContainerStyle>
            </ComboBox>
            <Border x:Name="color_swatch_usage_type"
                    Grid.Column="1"
                    Width="30"
                    Height="30"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Background="Transparent"
                    CornerRadius="3"/>
        </Grid>
        
        <TextBlock Text="Usage Type Prev:" 
                   FontSize="14" 
                   FontWeight="Bold"
                   Margin="0,0,0,5"/>
        
        <Grid Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="combo_usage_type_prev" 
                      Grid.Column="0"
                      Height="30"
                      FontSize="13"
                      IsEditable="True"
                      IsTextSearchEnabled="False"
                      Margin="0,0,10,0"
                      FlowDirection="RightToLeft"
                      Background="White">
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ComboBox.ItemContainerStyle>
            </ComboBox>
            <Border x:Name="color_swatch_usage_type_prev"
                    Grid.Column="1"
                    Width="30"
                    Height="30"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Background="Transparent"
                    CornerRadius="3"/>
        </Grid>
        
        <TextBlock x:Name="text_info" 
                   Text="No area elements selected"
                   FontSize="11"
                   Foreground="Gray"
                   Margin="0,0,0,10"/>
    </StackPanel>
        
        <!-- Extensible Schema Fields Section -->
        <Border Grid.Row="1" 
                Background="White"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="3"
                Margin="0,10,0,0"
                Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Disabled" Background="White">
                <StackPanel x:Name="panel_schema_fields" Background="White"/>
            </ScrollViewer>
        </Border>
        
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button x:Name="btn_apply" 
                    Content="Apply" 
                    Width="80" 
                    Height="30"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="apply_clicked"/>
            <Button x:Name="btn_cancel" 
                    Content="Cancel" 
                    Width="80" 
                    Height="30"
                    IsCancel="True"
                    Click="cancel_clicked"/>
        </StackPanel>
    </Grid>
</Window>
