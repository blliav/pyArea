<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Define pyArea Schema" 
        Width="700" Height="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="28"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="28"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Element Information -->
        <GroupBox Grid.Row="0" Header="Element Information" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Element Type:"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="text_element_type" 
                           Text="AreaScheme" FontWeight="Bold"/>
                
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Count:" Margin="0,5,0,0"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="text_selected_count" 
                           Text="0 elements selected" Margin="0,5,0,0" FontWeight="Bold"/>
                
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Elements:" Margin="0,5,0,0"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="text_element_names" 
                           Text="" Margin="0,5,0,0" TextWrapping="Wrap" 
                           Foreground="DarkBlue" MaxHeight="60" 
                           ToolTip="Names of selected elements"/>
            </Grid>
        </GroupBox>
        
        <!-- Municipality Information (read-only for Sheet/AreaPlan) -->
        <GroupBox Grid.Row="1" x:Name="group_municipality" Header="Municipality" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Municipality:"/>
                <TextBlock Grid.Column="1" x:Name="text_municipality" 
                           Text="Not detected" FontWeight="Bold" Foreground="DarkGreen"/>
            </Grid>
        </GroupBox>
        
        <!-- Data Fields -->
        <GroupBox Grid.Row="2" Header="Data Fields" Padding="10" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="panel_fields">
                    <TextBlock Text="Select element type and municipality to see fields" 
                               FontStyle="Italic" Foreground="Gray"/>
                </StackPanel>
            </ScrollViewer>
        </GroupBox>
        
        <!-- Status Messages -->
        <GroupBox Grid.Row="3" Header="Status" Padding="10" Margin="0,0,0,10" Height="80">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="text_status" TextWrapping="Wrap" 
                           Text="Ready to define schema data..." Foreground="Gray"/>
            </ScrollViewer>
        </GroupBox>
        
        <!-- Action Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="1" x:Name="btn_load" Content="Load Data" 
                    ToolTip="Load existing data from first selected element"/>
            <Button Grid.Column="2" x:Name="btn_apply" Content="Apply" 
                    ToolTip="Apply data to selected elements"/>
            <Button Grid.Column="3" x:Name="btn_close" Content="Close" IsCancel="True"/>
        </Grid>
    </Grid>
</Window>
