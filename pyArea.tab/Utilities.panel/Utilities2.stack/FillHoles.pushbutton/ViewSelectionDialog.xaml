<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Area Plan Views" 
        Width="600" Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Area Scheme Selection -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Area Scheme:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="combo_areascheme" 
                      Height="30" 
                      FontSize="13"
                      ToolTip="Select the area scheme for filtering views"/>
        </StackPanel>
        
        <!-- Instructions -->
        <TextBlock Grid.Row="1" 
                   Text="Select the Area Plan views to process:" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>
        
        <!-- Area Plan Views List -->
        <Border Grid.Row="2" 
                BorderBrush="LightGray" 
                BorderThickness="1" 
                CornerRadius="3"
                Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="panel_views"/>
            </ScrollViewer>
        </Border>
        
        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button x:Name="btn_select_all" 
                    Grid.Column="0"
                    Content="Select All" 
                    Width="100" 
                    Height="30"
                    Margin="0,0,10,0"/>
            
            <Button x:Name="btn_ok" 
                    Grid.Column="2"
                    Content="OK" 
                    Width="100" 
                    Height="30"
                    Margin="0,0,10,0"
                    IsDefault="True"/>
            
            <Button x:Name="btn_cancel" 
                    Grid.Column="3"
                    Content="Cancel" 
                    Width="100" 
                    Height="30"
                    IsCancel="True"/>
        </Grid>
    </Grid>
</Window>
